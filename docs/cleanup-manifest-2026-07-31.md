# Cleanup manifest — 2026-07-31 (handover pass)

Second cleanup pass on branch `tree-only`, extending the earlier `archive/` pass
(`archive/ARCHIVE_MANIFEST.md`) and the `testing/` parking (`testing/MANIFEST.md`).
Everything below is staged, nothing committed. `config/application.yml` and `.env`
untouched.

## 1. Files moved / untracked / deleted

| Action | Path(s) | Reason |
|---|---|---|
| archive | 19 untracked `experiment_configs/arena_*_{newlogic,qfix}.toml`, `arena_history_*` variants, `arena_math_12model.toml` | pre-baseline exploration era (outputs already parked in `testing/`); superseded by r8 |
| archive (git mv) | `experiment_configs/{mcs30_eps02_test,mcs30_eps02_tree,mcs30_eps02_tree_treefix,bar025,smoke_arena_v2,thesis_canonical,arena_math_rerun}.toml` | superseded calibration/exploration configs |
| archive | `experiment_results/{arena_history_resolved,arena_history_resolved_s137,thesis_canonical,bar025,mcs30_eps02_test,mcs30_eps02_tree,mcs30_eps02_tree_treefix}` | result folders of the archived configs |
| archive | `ratings_history_resolved*.db` (3 stores + sidecars) | validation-era; superseded by `r8/history` |
| archive | `ratings_r8_history.db.TRIAL` | trial preceding the final r8 history run |
| archive | `ratings_baseline_{math,physics}.db-shm/-wal` | orphaned sidecars, base DBs gone |
| archive | `docs/compass_artifact_wf-*.md` | imported research dump, not a deliverable |
| archive | `.claude/tmp_*.py`, `.claude/{abstention_triples,ladder,rubric_blindness_cells}.json` → `archive/analysis_scratch/` | session analysis scratch, kept for provenance |
| archive (git mv) | `tools/tuning/sweep_spec.dagmax.toml.bak` → `archive/tuning/` | DAG-era spec backup |
| delete | `snapshots_test.db`, `frozen_triples_stubbed-snapshot_MAIN.json` | regenerated by any test run |
| delete | `tools/tuning/__pycache__/` (git rm --cached + rm), `tools/analysis/__pycache__/` | bytecode caches; now gitignored |
| gitignore | `.mcp.json`, `.claude/`, `lancedb/`, `__pycache__/`, `*.pyc`, `testing/`, `archive/**` (manifests re-included), `*.db.*`, `*_secured/`, `model_domain_scores.csv`, `reserved_leaf_assignments.csv`, `tuning/runs/*/output/` | local tooling / parked evidence / regenerable exports stay on disk, out of git |
| keep + stage | `experiment_configs/r8_*.toml` (8), `freeze_mcs55.toml`, `verify_agree.toml`, `baseline_*.toml` (8) | reproducibility path of the thesis batch and the no-taxonomy baseline |
| keep + stage | `tools/analysis/{azure_concurrency_sweep,azure_ratelimit_probe,cell_heterogeneity_dl,hierarchical_bt,rubric_specificity_null,trace_redaction_audit}.py`, `src/test/.../RandomCellRubricNullHarness.kt`, untracked `docs/*.md`, `archive/{ARCHIVE_MANIFEST,README}.md` | handover deliverables previously untracked |
| keep (unmoved) | `ratings.db`, `snapshots.db`, `embeddings_cache.db`, `mmlu_pro_dataset_cache_v2.db`, `ratings_*_paired.db` (6) + `ratings_math_paired.db.final`, `*_secured/` (5), `experiment_results/{arena_math_frozen,arena_math_paired,canonical_baseline,canonical_freeze,freeze_mcs55,cal_iso,cal_seeds_bar025}` | read by `report/Figures/*.py` / `tools/analysis/*` / cited by the thesis (see `testing/MANIFEST.md` § Deliberately left in place) |

## 2. Result reorganisation

`experiment_results/r8/<domain>/` now holds each run's `ratings_r8_<domain>.db`
(+sidecars) beside its `seed_42/` exports; `verify_agree_philosophy/` (with
`ratings_verify_agree.db`) sits alongside. `experiment_results/r8/README.md`
(staged) documents run dates, configs, comparisons and the shared snapshot id
`20260727_042523_Headless_Run_Auto_ge`. Path consumers updated in the same change:
`report/Figures/make_routing_provenance.py:70`, `make_rubric_reach.py:112`,
`report/Figures/FIGURE_PLAN.md` (F1/F2 data paths). No other repo reference to
`ratings_r8_*.db` exists (verified by grep).

## 3. Comment cleanup (session narration → invariants)

| File | Comments rewritten/removed |
|---|---|
| `service/TaxonomyBenchmarkService.kt` | 8 (incl. the reverted-widening essay → one-cell invariant; `judgeAccuracyAgreement` note → decidable-only invariant) |
| `service/TaxonomyRankingService.kt` | 2 ("WHAT THIS REPLACED" blocks → pooled-agreement and SE_ivw rationale) |
| `service/BtStoppingPolicy.kt` | 5 (Hoeffding autopsy, budget key, per-cell gate, arm matching x2) |
| `service/BtMatchScheduler.kt` | 3 (bootstrap floor, BOOT key rotation, pool shuffle) |
| `service/ActiveBtRacingScheduler.kt` | 6 (exact binomial, effectiveN, ranking, pairStatus, pickQuery, batch quotas) |
| `service/BtMmFitter.kt` | 2 (1/K² correction, Jeffreys note) |
| `operations/ArcTaxonomyLLMClient.kt` | 7 (fallback RPS, pacing, token budget, ARENA-LAT, AIMD, delay signal, timeout) |
| `operations/TaxonomySplitter.kt` | 6 (removed probe/margin/joint-gate/sibling-guard notes → live invariants) |
| `operations/TaxonomyOperations.kt` | 4 (residual retention, fingerprint components) |
| `operations/TaxonomyTrickler.kt` | 3 (nearMisses ledger deleted, topo walk, header) |
| `operations/TaxonomyFitter.kt` / `TaxonomyMerger.kt` | 1 + 1 (EMA, isBridge) |
| `config/TaxonomyConfig.kt` | 2 (dated sentence, membershipFloor) |
| `runner/HeadlessBenchmarkRunner.kt` | 2 (enable* flags note, J export) |
| `service/TaxonomyArenaService.kt` | 1 (removed dead-prompt-line paragraph) |
| `dataset/ReservedPool.kt` / `ModelEvalStore.kt` | 1 + 1 (durability rationale to present tense) |
| `diagnostics/DiagnosticsBundle.kt` | 1 (empty-file note; the proposals.csv COMPATIBILITY BREAK note kept — needed to read archived CSVs) |
| tests: `BtVarianceCorrectionTest`, `TraceEnvelopeUnwrapTest`, `BtSchedulerRedesignTest`, `ReservedPoolTest`, `ReservedPoolSyncTest` | 5 (dates/narration dropped, guard intent kept) |

Kept deliberately: id-space warnings (three id spaces), threading/units notes, the
`DiagnosticsBundle` CSV compatibility break, `sweep_spec.toml` calibration header.

## 4. Dead code and tests removed

| Item | Evidence |
|---|---|
| `TaxonomyBenchmarkService.processPrecomputedQuery` (173 lines) + private `data class PairResult` | IDEA inspection "never used"; `grep -rn processPrecomputedQuery src/` → definition only; private, so no Spring/reflection entry point. `PairResult` was used only inside it. |
| 5 unused imports in `TaxonomyBenchmarkService.kt` (`Semaphore`, `withPermit`, `jsonObject`, `jsonPrimitive`, `kotlin.math.exp`) | IDEA "Unused import directive"; compile clean after removal. |
| Tests removed: **none** | All 218 tests target live mechanisms (bridging is off but kept+tested by design; `canonical_freeze.toml`, cited by two tests, was kept). Test-DB isolation untouched. |

## 5. Verification

| Check | Result |
|---|---|
| `./gradlew compileKotlin compileTestKotlin` | exit 0 |
| `./gradlew test` | BUILD SUCCESSFUL — 218 tests, 0 failures, 0 skipped (baseline: 218/0) |
| `make_tree_full.py`, `make_judge_example.py`, `make_frozen_tree.py`, `make_leaf_size_hist.py` | all emit their PDFs; frozen-tree verification "Structure reproduces" |
| `make_routing_provenance.py`, `make_rubric_reach.py` (updated paths) | both emit their PDFs from `experiment_results/r8/` and reproduce published values |

## Second pass — import sweep

All 185 Kotlin files scanned (word-boundary usage search over each file's body,
wildcard imports and operator/convention names excluded from candidacy); 37 unused
imports removed across 25 files. Verified by full compile + test, not by the heuristic
alone. No further narration comments found in the previously untouched files
(TUI, controllers, dataset, utils) — zero comment rewrites this pass. `report/`
untouched.

| File | Imports removed |
|---|---|
| `config/TaxonomyConfig.kt` | `taxonomy.model.GraphNode` |
| `controller/{ModelEval,TaxonomyArena,Taxonomy,TaxonomyJudge}Controller.kt` (4 files) | `kotlinx.coroutines.runBlocking` (each) |
| `operations/TaxonomyMerger.kt` | `JsonObjectSchema`, `JsonSchema`, `TaxoPrompts` |
| `service/BtMatchScheduler.kt` | `taxonomy.utils.StatisticsUtils` |
| `service/TaxonomyArenaService.kt` | `taxonomy.model.projectTo`, `kotlin.math.abs` |
| `tui/app/TuiRouter.kt` | `Color.Companion.Yellow`, `ConfigSubPanel` |
| `tui/components/TopologyTables.kt` | `mosaic.layout.padding` |
| `tui/controller/CommandController.kt` | `ConfigSubPanel` |
| `tui/controller/keys/AnalysisKeyHandler.kt` | `BenchmarkSection` |
| `tui/controller/keys/ConfigKeyHandler.kt` | `ConfigSubPanel` |
| `tui/features/arena/ArenaPanel.kt` | `mosaic.modifier.Modifier` |
| `tui/features/benchmark/BenchmarkPanel.kt` | `HotkeyAction`, `DomainSelectorTable`, `SettingItem`, `SettingKind`, `BenchmarkLiveView`, `BenchmarkSection` |
| `tui/features/logs/LogsPanel.kt` | `TextStyle.Companion.Bold` |
| `tui/features/startup/WelcomePanel.kt` | `mosaic.layout.padding`, `mosaic.modifier.Modifier` |
| `test/KappaBiasCorrectionTest.kt` | `kotlin.math.abs` |
| `test/RankingServiceTest.kt` | `java.sql.DriverManager` |
| `test/SoftMembershipBtScoresTest.kt` | `Assertions.assertEquals` |
| `test/TaxonomyMetricsConsolidatedTest.kt` | `Assertions.assertNotNull`, `Assertions.assertNotEquals` |
| `test/TournamentSimulatorTest.kt` | `java.sql.DriverManager` |
| `test/service/BtSchedulerRedesignTest.kt` | `ModelRank`, `TaxonomyRankingService.AggregatedLeaderboard` |
| `test/tui/EvalCatalogPickerReducerTest.kt` | `BenchmarkUiState` |
| `test/tui/features/analysis/MetricsOrInspectorPanelTest.kt` | `Color.Companion.Green` |

Verification: `./gradlew compileKotlin compileTestKotlin` exit 0;
`./gradlew test` BUILD SUCCESSFUL — 218 tests, 0 failures, 0 skipped (baseline preserved).
